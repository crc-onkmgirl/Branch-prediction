To compile the code, simply use command "make".

Saturating-counter's parameters m and k are read from config.txt. For different m and k, have to edit config file.

The code reads trace.txt line by line, and outputs the prediction of PHT for current instruction in the pc in trace.txt.out in corresponding line.

